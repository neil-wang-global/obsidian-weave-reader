## 改进

- **Canvas 摘录锚点**：可在 Canvas 节点右键固定或取消固定阅读器摘录锚点，后续摘录按锚点链式布局
- **摘录布局方向**：Canvas 节点与选区菜单新增「阅读器摘录方向」，支持上 / 下 / 左 / 右四个方向
- **脑图摘录联动**：摘录节点创建与布局逻辑优化，锚点状态持久化到阅读器存储

## 修复

- 移除未使用的 `monkey-around` 依赖，减小安装体积
